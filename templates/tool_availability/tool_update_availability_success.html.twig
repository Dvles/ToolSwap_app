{% extends 'base.html.twig' %}

{% block title %}Disponibilités modifiées!{% endblock %}

{% block body %}
<div class="container d-flex flex-column justify-content-center align-items-center text-center" style="height: 72vh;">
    <div class="card shadow-lg p-4" style="max-width: 600px;">
        <h1 class="fw-bold mb-3">Disponibilités modifiées!</h1>
        <p class="lead mb-4">Bravo! Mise à jour des disponibilités réussie.</p>
        <p class="text-muted">Vous serez redirigé(e) automatiquement dans <span id="countdown" class="fw-bold">5</span> secondes...</p>

        <div class="d-flex justify-content-center mt-3">
            <div class="spinner-border text-warning" role="status">
                <span class="visually-hidden">Loading...</span>
            </div>
        </div>
    </div>
</div>

<script>
    let countdown = 5;
    const countdownElement = document.getElementById('countdown');

    setInterval(() => {
        if (countdown > 1) {
            countdown--;
            countdownElement.textContent = countdown;
        } else {
            window.location.href = '{{ path('tool_display_user') }}'; // Redirect after countdown
        }
    }, 1000); // 1 second interval
</script>
{% endblock %}
