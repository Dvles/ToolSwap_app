{% extends 'base.html.twig' %}

{% block title %}Commenter outil{% endblock %}

{% block body %}
    <h1>Commenter outil</h1>

    {{ form_start(form) }}

    {{ form_label(form.rating) }}<br>
    {{ form_widget(form.rating) }}<br>

    {{ form_label(form.comment) }}<br>
    {{ form_widget(form.comment) }}<br>

    <input type="submit" value="Insérer">
    
    {{ form_end(form) }}
    
{% endblock %}