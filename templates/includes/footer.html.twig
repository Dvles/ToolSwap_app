<footer style="background-color: #f8f9fa; padding: 20px; text-align: center; border-top: 1px solid #e7e7e7; margin-top: 40px;">
    <nav style="margin: 0 auto; display: flex; flex-direction: column; align-items: center; gap: 10px;">
        <!-- Branding -->
        <a href="{{ path('tool_display_all') }}" style="color: black; font-weight: bold; font-size: 20px; text-decoration: none;">
            TOOLSWAP©
        </a>

        <!-- Links -->
        <div style="display: flex; gap: 15px; flex-wrap: wrap; justify-content: center; font-size: 16px;">
            <a href="{{ path('coming_soon') }}" style="color: #007bff; text-decoration: none;">À propos</a>
            <a href="{{ path('coming_soon') }}" style="color: #007bff; text-decoration: none;">Contactez-nous</a>
            <a href="{{ path('coming_soon') }}" style="color: #007bff; text-decoration: none;">FAQ</a>
            <a href="{{ path('coming_soon') }}" style="color: #007bff; text-decoration: none;">Conditions d'utilisation</a>
            <a href="{{ path('coming_soon') }}" style="color: #007bff; text-decoration: none;">Politique de confidentialité</a>
        </div>

        <!-- Search Bar -->
        <form class="form-inline" action="{{ path('tool_search') }}" method="get" style="margin-top: 10px; display: flex; gap: 10px;">
            <input class="form-control" type="search" placeholder="Rechercher des outils" aria-label="Rechercher" name="keyword" required
                style="padding: 8px; border-radius: 4px; border: 1px solid #ced4da; width: 200px;">
            <button class="btn btn-outline-success" type="submit" style="padding: 8px 12px; font-size: 14px;">Rechercher</button>
        </form>

        <!-- Social Media Links -->
        <div style="margin-top: 15px; font-size: 14px; color: #6c757d;">
            <a href="{{ path('coming_soon') }}" style="margin-right: 10px; text-decoration: none; color: #007bff;">Facebook</a>
            <a href="{{ path('coming_soon') }}" style="margin-right: 10px; text-decoration: none; color: #007bff;">Twitter</a>
            <a href="{{ path('coming_soon') }}" style="text-decoration: none; color: #007bff;">Instagram</a>
        </div>
    </nav>
</footer>
